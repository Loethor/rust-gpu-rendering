[package]
name = "rust_gpu_rendering"
version = "0.1.2"
edition = "2021"

[dependencies]
image = { version = "0.24", optional = true }
rand = "0.8"

[features]
png = ["dep:image"]

[dev-dependencies]
image = "0.25.10"
plotters = "0.3"